# 命名空间与物理目录迁移审计

## 1. 目标

- 坐标：`T / R / M / G / TH / TY`
- 知识网络：`IM / CN`
- 物理目录：`10 作品坐标系统/` + `20 作品知识网络/`
- legacy Q 仅保留为迁移 provenance，不再作为 canonical 目录或字段。

## 2. 首次物理迁移

首次从旧 `10 轴/`、`20 节点/` 执行物理目录与命名空间迁移时：

- 物理 move / rename 记录：**425**
- 内容重写文件：**4784**
- 结构错误：**0**

该数字描述首次 materialize 过程，不应被后续幂等重跑覆盖。

## 3. 收口重跑

在 PR 审查阶段，迁移器再次以已完成的物理结构为输入运行：

- 新增物理 move / rename：**0**
- 需要再次规范化的内容文件：**110**
- 导航 / namespace 收口修复：**67** 个文件级变更
- v2 架构校验：**PASS / errors=0**
- `git diff --check`：**PASS**
- 两个顶层 Canvas JSON：**PASS**

这些重跑用于确认迁移在新结构上可收敛，并修复搬家后的相对路径与根节点 metadata；不代表首次迁移只有 0 次 move。

## 4. 最终作品字段状态

- `type: work`：**4062**
- `axis_th`：**3964**
- `axis_ty`：**3964**
- `imagery`：**299**
- `cultural_narrative_relations`：**0**
- `legacy_axis_q`：**3964**

## 5. 退役 canonical 字段残留

- `axis_qh`：**0**
- `axis_qt`：**0**
- `qx`：**0**
- `qc_relations`：**0**

长期 validation 另外禁止 `axis_q` 重新作为 canonical work field 出现；迁移前的原始 QH / QT 合并值只存在于 `legacy_axis_q`。

## 6. 最终物理结构

```text
30 世界文学/
├─ 10 作品坐标系统/
│  ├─ T 时间/
│  ├─ R 地域/
│  ├─ M 思潮与美学/
│  ├─ G 体裁/
│  ├─ TH 主题/
│  └─ TY 类型与叙事机制/
├─ 20 作品知识网络/
│  ├─ IM 意象/
│  └─ CN 文化叙事/
├─ 30 专题/
└─ 40 作品/
```

旧 `10 轴/`、`20 节点/` 已退出 canonical 物理结构，不保留空壳目录。

## 7. legacy provenance

机械映射：

```text
QH -> TH
QT -> TY
QX -> IM
QC -> CN
```

节点使用 `legacy_code` / `legacy_codes`、`legacy_id` 保存旧编号；CN 节点若在更早阶段已有 QT8 等来源，保留多代 provenance，不覆盖历史。

作品使用 `legacy_axis_q` 保存迁移前原始 QH / QT 合并值，仅供审计，不参与 canonical 坐标检索。

## 8. 治理结论

本次迁移只改变命名空间、物理位置、字段名和引用路径：

- 未新增 TH / TY / IM / CN taxonomy 概念；
- 未扩建 CN3；
- 未强制补 IM / CN；
- 未自动建立 `cultural_narrative_relations`；
- 未重新设计 T / R / M / G taxonomy。

后续以 `literature-architecture-validation-v2` 作为长期只读门禁，防止旧 Q 命名和旧物理目录重新成为 canonical。
